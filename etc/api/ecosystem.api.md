## API Report File for "@yugao-gaos/gaos-sdk"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public (undocumented)
interface ActionDefinition {
    // (undocumented)
    id: string;
    params: 'none' | 'xy' | 'index' | 'targets';
    // (undocumented)
    targetSpecId?: string;
    // (undocumented)
    text?: string;
}

// @public
type Cell = [number, number];

// @public
type GridTargetingView = GridViewNamespace | Readonly<Record<string, GridViewNamespace>>;

// @public (undocumented)
interface GridViewNamespace {
    // (undocumented)
    actionTargeting?: Readonly<Record<string, {
        targetableCells: readonly Cell[];
    }>>;
    // (undocumented)
    targetableCells?: readonly Cell[];
}

// @public (undocumented)
export const HOST_CONFORMANCE_FIXTURE_VERSION: "gaos.host-conformance-fixture.v1";

// @public (undocumented)
export const HOST_CONFORMANCE_VERSION: "gaos.host-conformance.v1";

// @public (undocumented)
export interface HostArtifact {
    // (undocumented)
    artifact: JsonValue;
    // (undocumented)
    format: string;
}

// @public (undocumented)
export interface HostConformanceAdapter {
    // (undocumented)
    adapterVersion: string;
    // (undocumented)
    execute(fixture: HostConformanceFixture): Promise<JsonValue>;
    // (undocumented)
    runtime: string;
}

// @public (undocumented)
export interface HostConformanceFixture {
    // (undocumented)
    scenario: Rfc013HostConformanceScenario;
    // (undocumented)
    schema: typeof HOST_CONFORMANCE_FIXTURE_VERSION;
    // (undocumented)
    steps: readonly {
        sequence: number;
        operation: string;
    }[];
}

// @public (undocumented)
export interface HostConformanceReport {
    // (undocumented)
    adapterVersion: string;
    // (undocumented)
    passed: boolean;
    // (undocumented)
    runtime: string;
    // (undocumented)
    scenarios: readonly {
        scenario: Rfc013HostConformanceScenario;
        passed: boolean;
        details?: JsonValue;
    }[];
    // (undocumented)
    schema: typeof HOST_CONFORMANCE_VERSION;
}

// @public (undocumented)
export interface HostCreateInput {
    // (undocumented)
    [key: string]: unknown;
    // (undocumented)
    sessionId: string;
}

// @public (undocumented)
export interface HostedSession<TCommand, TView> {
    // (undocumented)
    ingest(input: HostSubmission<TCommand>): Promise<void>;
    // (undocumented)
    sessionId: string;
    // (undocumented)
    snapshot(seat: string, afterRevision?: number): Promise<HostObservation<TView>>;
}

// @public (undocumented)
export interface HostObservation<TView> {
    // (undocumented)
    observation: SnapshotResult<TView>;
}

// @public (undocumented)
export interface HostSeatControl {
    // (undocumented)
    authorization: unknown;
    // (undocumented)
    changes: readonly unknown[];
}

// @public (undocumented)
export interface HostSubmission<TCommand> {
    // (undocumented)
    [key: string]: unknown;
    // (undocumented)
    command: TCommand;
}

// @public (undocumented)
interface JsonObject {
    // (undocumented)
    [key: string]: JsonValue;
}

// @public (undocumented)
type JsonPatchOperation = {
    op: 'add' | 'replace';
    path: string;
    value: JsonValue;
} | {
    op: 'remove';
    path: string;
};

// @public (undocumented)
type JsonPrimitive = string | number | boolean | null;

// @public (undocumented)
type JsonValue = JsonPrimitive | JsonObject | JsonValue[];

// @public
type LocationCoord = Cell | string | number;

// @public
interface LocationRef {
    // (undocumented)
    container: string;
    // (undocumented)
    coord: LocationCoord;
}

// @public (undocumented)
interface ObservationAcknowledgement {
    // (undocumented)
    participantId: string;
    // (undocumented)
    submissionId: string;
}

// @public (undocumented)
interface ObservationDelta<TView = TickView<unknown, unknown>> {
    acknowledgements: readonly ObservationAcknowledgement[];
    // (undocumented)
    body: {
        kind: 'snapshot';
        view: TView;
    } | {
        kind: 'patch';
        operations: readonly JsonPatchOperation[];
    } | {
        kind: 'unchanged';
    };
    // (undocumented)
    codec: 'v2';
    interest?: {
        declaration: JsonValue;
    };
    origin?: 'resolution' | 'snapshot' | 'interest';
    rejections: readonly ObservationRejectionNotice[];
    scopeId?: string;
    // (undocumented)
    seat: string;
    // (undocumented)
    tick: number;
    transitionRevision: number;
    viewDigest: number;
    // (undocumented)
    viewRevision: number;
}

// @public (undocumented)
interface ObservationRejectionNotice {
    // (undocumented)
    code: 'commit_mismatch';
    // (undocumented)
    participantId: string;
    seat: string;
    // (undocumented)
    submissionId: string;
    // (undocumented)
    tick: number;
    // (undocumented)
    transitionRevision: number;
}

// @public (undocumented)
type Outcome = {
    kind: 'ongoing';
} | {
    kind: 'decided';
    ranking: ReadonlyArray<{
        seat: string;
        rank: number;
        score?: number;
    }>;
    reason?: string;
};

// @public (undocumented)
type Participation = {
    mode: 'sequential';
    activeSeat: string;
} | {
    mode: 'simultaneous';
    seats: readonly string[];
};

// @public (undocumented)
export interface PresentationEvent {
    // (undocumented)
    [key: string]: unknown;
    id: string;
    // (undocumented)
    type: string;
}

// @public (undocumented)
export interface PresentationFrame<TView, TEvent> {
    // (undocumented)
    events: readonly TEvent[];
    // (undocumented)
    repair?: boolean;
    // (undocumented)
    stateDigest?: string;
    // (undocumented)
    tick: number;
    // (undocumented)
    transitionRevision: number;
    // (undocumented)
    view: TView;
}

// @public
export function presentationFrameFromObservation<TView, TEvent extends PresentationEvent>(delta: ObservationDelta<TView>, view: TView, events: readonly TEvent[], options?: {
    stateDigest?: string;
    repair?: boolean;
}): PresentationFrame<TView, TEvent>;

// @public (undocumented)
export const RFC013_HOST_CONFORMANCE_SCENARIOS: readonly ["byte-identical retry", "conflicting event reuse", "crash before persistence", "crash after persistence", "crash after commit", "publish retry after durable commit", "stale prepared transition rejection", "timeout transition handling", "acknowledgement and rejection", "reconnect repair", "patch without base snapshot", "dropout and drop-in", "reconnect and substitution", "transfer and atomic seat swap", "inactive controller epoch rejection", "checkpoint restore and retention floor", "artifact finalization and independent verification"];

// @public (undocumented)
export type Rfc013HostConformanceScenario = typeof RFC013_HOST_CONFORMANCE_SCENARIOS[number];

// @public (undocumented)
export const RFC014_HOST_CONFORMANCE_FIXTURES: readonly HostConformanceFixture[];

// @public (undocumented)
export const RFC014_HOST_CONFORMANCE_SCENARIOS: readonly ["byte-identical retry", "conflicting event reuse", "crash before persistence", "crash after persistence", "crash after commit", "publish retry after durable commit", "stale prepared transition rejection", "timeout transition handling", "acknowledgement and rejection", "reconnect repair", "patch without base snapshot", "dropout and drop-in", "reconnect and substitution", "transfer and atomic seat swap", "inactive controller epoch rejection", "checkpoint restore and retention floor", "artifact finalization and independent verification"];

// @public
export function runHostConformance(adapter: HostConformanceAdapter, fixtures?: readonly HostConformanceFixture[]): Promise<HostConformanceReport>;

// @public
export function runReferenceHostConformance(): Promise<HostConformanceReport>;

// @public
export interface SessionHostDriver<TCommand, TView> {
    // (undocumented)
    advance(sessionId: string, tick: number): Promise<void>;
    // (undocumented)
    control(sessionId: string, input: HostSeatControl): Promise<void>;
    // (undocumented)
    create(input: HostCreateInput): Promise<HostedSession<TCommand, TView>>;
    // (undocumented)
    ingest(sessionId: string, input: HostSubmission<TCommand>): Promise<void>;
    // (undocumented)
    snapshot(sessionId: string, seat: string, afterRevision?: number): Promise<HostObservation<TView>>;
    // (undocumented)
    terminate(sessionId: string, reason: string): Promise<HostArtifact>;
}

// @public
interface SessionView {
    activeSeat?: string;
    outcome?: Outcome;
    participation?: Participation;
    // (undocumented)
    stars?: number;
    // (undocumented)
    status: 'playing' | 'won' | 'failed' | 'ended';
}

// @public (undocumented)
type SnapshotResult<TView> = ObservationDelta<TView> | SnapshotResyncRequired;

// @public (undocumented)
interface SnapshotResyncRequired {
    // (undocumented)
    currentTransitionRevision: number;
    // (undocumented)
    requestedTransitionRevision: number;
    // (undocumented)
    retentionFloor: number;
    // (undocumented)
    status: 'resync_required';
}

// @public (undocumented)
interface TargetChoiceView {
    // (undocumented)
    choices: readonly (readonly LocationRef[])[];
    truncated: boolean;
}

// @public
interface TickView<TGrid = GridTargetingView, TZones = ZoneViews> extends SessionView {
    // (undocumented)
    actions: readonly ActionDefinition[];
    grid?: TGrid;
    // (undocumented)
    hud: {
        actionsUsed: number;
        items?: ReadonlyArray<{
            index: number;
        }>;
        dialogueOptions?: ReadonlyArray<{
            index: number;
        }>;
        pois?: ReadonlyArray<{
            index: number;
        }>;
    };
    systemActions?: readonly ActionDefinition[];
    targetChoices?: Readonly<Record<string, TargetChoiceView>>;
    zones?: TZones;
}

// @public (undocumented)
interface ZoneEntryView {
    // (undocumented)
    [key: string]: unknown;
    // (undocumented)
    id: string;
}

// @public (undocumented)
interface ZoneViewNamespace<TEntry = ZoneEntryView> {
    // (undocumented)
    [key: string]: unknown;
    count: number;
    // (undocumented)
    entries?: readonly TEntry[];
    ordered?: boolean;
    // (undocumented)
    slots?: Readonly<Record<string, TEntry | null>>;
}

// @public (undocumented)
type ZoneViews<TEntry = ZoneEntryView> = Readonly<Record<string, ZoneViewNamespace<TEntry>>>;

// (No @packageDocumentation comment for this package)

```
